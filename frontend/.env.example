# =====================================================
# Frontend (.env.example) — copy to .env and fill in.
# =====================================================

# Base URL of the NestJS backend.
# Keep port 3000 unless you also change backend PORT.
NEXT_PUBLIC_API_URL=http://localhost:3000
